---
id: "3-2"
title: "实时搜索"
page_title: "输入即反馈：给作品页加上实时搜索"
seo_title: "React 实时搜索教程 - 输入即过滤的交互效果"
seo_description: "用 AI 给作品集网站加上实时搜索和标签过滤功能，实现输入即反馈的现代交互体验。"
keywords: ["react实时搜索", "前端搜索过滤", "AI做搜索功能", "输入即反馈", "标签过滤教程"]
chapter: "chapter_3"
order: 2
is_delivery: false
verification_type: "checklist"
verification_config: {}
estimated_minutes: 15
---

你在淘宝搜索框打字时，下面的结果立刻就变了——不需要点"搜索"按钮。这种"输入即反馈"的体验，是现代应用的标配。

这一关在作品页加上这个功能。

## 操作步骤

### 第一步：加搜索框

对 AI 说：

```text
在作品页（/works）的顶部加一个搜索框：

- 宽度占满内容区域，最宽 480px，居中
- 左侧有搜索图标
- placeholder 文字："搜索作品名称或描述..."
- 圆角，有边框，focus 时边框变成强调色
- 有清除按钮（输入内容后出现，点击清空）
```

---

### 第二步：实现实时过滤

对 AI 说：

```text
实现实时搜索过滤功能：

- 用户输入关键词时，作品卡片实时过滤
- 过滤规则：卡片的标题或描述包含关键词就显示，否则隐藏
- 过滤时有淡入淡出动画（卡片消失和出现都有过渡）
- 搜索框清空时恢复显示所有卡片
- 没有匹配结果时，显示一个友好的空状态（比如"没有找到相关作品"）
```

在搜索框里输入一些关键词，看看卡片是否实时过滤。

---

### 第三步：加标签过滤

对 AI 说：

```text
在搜索框下方加上标签过滤：

- 从作品数据中提取所有标签，显示为可点击的标签按钮
- 点击标签后，只显示包含该标签的作品
- 选中的标签有高亮样式
- 可以和搜索框组合使用（同时满足搜索词和标签）
- 点击已选中的标签取消选中
```

## 通关条件

完成以下三步即可通关：

1. 加搜索框，输入内容后有清除按钮
2. 实时过滤正常工作，有动画效果
3. 搜索效果符合预期（输入关键词，不相关的卡片消失）

## 卡住了？

- 搜索不生效？让 AI 检查事件监听是否绑定到了正确的输入框元素
- 过滤逻辑不对？告诉 AI 具体是什么情况——比如"输入'设计'但设计相关的卡片也消失了"
- 动画不流畅？让 AI 用 CSS `transition` 配合 `opacity` 和 `transform` 实现，避免用 `display: none`
