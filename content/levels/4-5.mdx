---
id: "4-5"
title: "部署第四版"
page_title: "交付第四版：带后端能力的完整作品集"
seo_title: "Vercel 环境变量配置 - 部署带数据库的 Next.js 应用"
seo_description: "把带有 API 路由、数据库、后台管理和访问统计的作品集网站部署到 Vercel，配置生产环境变量。"
keywords: ["vercel环境变量", "nextjs数据库部署", "supabase生产环境", "全栈应用部署"]
chapter: "chapter_4"
order: 5
is_delivery: true
verification_type: "url_submit"
verification_config: {}
delivery_prompt: "粘贴你的 Vercel 网址（例如 https://xxx.vercel.app）"
estimated_minutes: 15
---

这一章你的网站从纯前端变成了全栈应用：有 API 接口、有数据库、有后台管理、有访问统计。

部署这样的应用，需要多做一步——配置生产环境的环境变量。

## 操作步骤

### 第一步：配置 Vercel 环境变量

本地开发时，环境变量放在 `.env.local` 文件里。但这个文件不会提交到 GitHub，所以 Vercel 不知道这些配置。

1. 打开 [vercel.com](https://vercel.com)，进入你的项目
2. 点击 "Settings" → "Environment Variables"
3. 添加以下变量：
   - `NEXT_PUBLIC_SUPABASE_URL`：你的 Supabase 项目 URL
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`：你的 Supabase anon key
   - `RESEND_API_KEY`：你的 Resend API Key（如果配置了邮件功能）

每个变量填写名称和值，Environment 选择 "Production"，点击 Save。

---

### 第二步：提交并推送

```bash
git add .
git commit -m "feat: API路由、数据库、后台管理、访问统计"
git push
```

---

### 第三步：验证生产环境

部署完成后，验证以下功能在线上正常工作：

- 作品页能正常加载（数据从数据库读取）
- 点击作品，查看次数增加
- 联系表单提交后有成功反馈
- 访问 `/admin/works` 能看到后台管理页面

如果某个功能在线上不工作，在 Vercel 的 "Functions" 标签里查看日志，把错误信息告诉 AI。

## 通关条件

完成以下三步即可通关：

1. Vercel 环境变量已配置
2. 线上作品页能正常加载数据库里的作品
3. 线上点击作品后查看次数增加

## 第四章完成！

你的作品集现在是一个真正的全栈应用：

- API 路由处理后端逻辑
- Supabase 数据库存储数据
- 后台管理页面管理内容
- 访问统计了解访客行为

下一章，我们来做最有意思的部分——接入 AI 能力，让网站真正"智能"起来。
