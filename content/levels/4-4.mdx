---
id: "4-4"
title: "访问统计"
page_title: "了解你的访客：给网站加上访问统计"
seo_title: "网站访问统计教程 - 自建数据看板"
seo_description: "用 Supabase 和 Next.js API 路由给作品集网站加上访问统计功能，了解哪些作品最受欢迎。"
keywords: ["网站访问统计", "自建数据看板", "nextjs统计功能", "AI做数据分析", "作品点击统计"]
chapter: "chapter_4"
order: 4
is_delivery: false
verification_type: "checklist"
verification_config: {}
estimated_minutes: 20
---

你的网站上线了，但你不知道有多少人来看，他们最感兴趣的是哪个作品。

这一关做一个简单的访问统计，让你对自己的网站有数据感知。

## 操作步骤

### 第一步：记录作品点击

对 AI 说：

```text
帮我给作品集加上点击统计：

1. 在 Supabase 里创建一个 work_views 表：
   - id（自增主键）
   - work_id（关联 works 表的 id）
   - viewed_at（查看时间，自动填充）
   
   给我 SQL 语句。

2. 创建 app/api/works/[id]/view/route.ts，POST 请求时在 work_views 表里插入一条记录

3. 在作品卡片上，用户点击"查看项目"或"查看详情"时，调用这个接口记录点击
```

在 Supabase 的 SQL Editor 里执行建表语句，然后点击几个作品，去 Table Editor 里确认数据有没有记录进去。

---

### 第二步：显示热门作品

对 AI 说：

```text
在作品页加上"热门作品"排序功能：

1. 更新 /api/works 接口，支持 ?sort=popular 参数
   - 默认按创建时间排序
   - sort=popular 时，按 work_views 表里的点击次数降序排列

2. 在作品页的排序选项里加上"最受欢迎"选项

3. 在每个作品卡片上显示查看次数（比如"👁 128 次查看"）
```

---

### 第三步：做数据看板

对 AI 说：

```text
在后台管理页面（/admin）加一个数据概览：

- 总作品数
- 本周总访问量（work_views 表里本周的记录数）
- 最受欢迎的作品（点击量最高的前 3 个，显示标题和点击数）
- 用简单的卡片布局展示这些数据

创建 app/api/admin/stats/route.ts 来提供这些统计数据。
```

## 通关条件

完成以下三步即可通关：

1. 点击作品后，work_views 表里有新记录
2. 作品卡片上显示查看次数
3. 后台管理页面有数据概览

## 卡住了？

- 点击没有记录？在浏览器控制台检查 API 请求是否发出，以及接口是否返回成功
- 统计数据不准确？让 AI 检查 SQL 查询语句，特别是 JOIN 和 COUNT 的写法
- 数据看板数据不更新？让 AI 在页面加载时调用统计接口，而不是用写死的数据
